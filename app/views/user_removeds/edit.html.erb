<h1>Editing user_removed</h1>

<% form_for(@user_removed) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :user_id %><br />
    <%= f.text_field :user_id %>
  </p>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @user_removed %> |
<%= link_to 'Back', user_removeds_path %>