<% form_for(@checkpoint) do |f| %>
  <%= f.error_messages %>
...<%= @checkpoint.goal.title %>

  <p>
    
  </p>
  <p>


  </p>
  <p>
  Add a Note about <%= @checkpoint.checkin_date.strftime("%A, %b. %d").to_s %>:<br><%= f.text_area :comment, :cols => 40, :rows => 2 %>
  </p>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<p>
<%= link_to '<-- Back (Cancel Update)', "/goals/single/#{@checkpoint.goal.id}" %>
</p>