<h1>My Brand New Team</h1>

<div class="alert" id="div_whether_team_is_category_or_goal_specific">
  <% if params[:team_type] == "category" %>

    This team will be for anyone with a goal in the <strong><%= @team.category_name %></strong> category. If you would rather restrict your team to work only on the goal of <strong><%= @goal.title %></strong>, <a href="/teams/new?team_type=goal&goal_id=<%= @goal.id %>">click here to make it a "goal-specific" team.</a>

  <% end %> <!-- end if params[:team_type] == "category" -->



  <% if params[:team_type] == "goal" %>

    <% if @template_goal %>

      This team will be for anyone working on the goal of <strong><%= @template_goal.title %></strong>. If you would rather invite anyone with a goal in the <strong><%= @goal.category %></strong> category to join, <a href="/teams/new?team_type=category&goal_id=<%= @goal.id %>">click here to make it a "<%= @goal.category %>" team.</a>

    <% else %>

      There was an error in saving or determining the template goal for this team. Please contact support@habitforge.com for assistance.

    <% end %> <!-- end if @template_goal -->

  <% end %> <!-- end if params[:team_type] == "goal" -->
</div> <!-- end div_whether_team_is_category_or_goal_specific-->


<% form_for(@team) do |f| %>
  <%= f.error_messages %>

  <%= f.hidden_field :goal_template_parent_id, :value => @team.goal_template_parent_id %>
  <%= f.hidden_field :category_name, :value => @team.category_name %>
  <%= f.hidden_field :custom, :value => @team.custom %>
  <%= f.hidden_field :owner_user_id, :value => @team.owner_user_id %>

  <% if params[:goal_id] %>
    <input type="hidden" name="goal_id" value="<%= params[:goal_id] %>">
  <% end %>

  <p>

  Team Name: <%= f.text_field :name, :placeholder => "ex: #{@example_team_name}" %>
  <br>
  Limit the size of this team to no more than <%= f.text_field :qty_max, :size => "2", :style => "width:20px;" %> people. (4 is shown to be best, but you can choose any number)
  </p>


  <h3>When sending invitations, use the text below:</h3>
  <br>Subject: 
  <br>
  <%= f.text_field :invitation_subject, :value => @team.invitation_subject, :style =>"width:600px;" %>
  <br>
  <br>
  Body:
  <br>
  <%= f.text_area :invitation_body, :value => @team.invitation_body, :cols => 40, :rows => 40, :style =>"width:600px;height:300px;" %>
  <br>
  (We will add a "Click to Join This Team Now" button at the end of the messages.)
  <p>
    <%= f.submit 'Create My Team', :class => "btn yellow" %>
  </p>
<% end %>

<%= link_to 'Cancel', teams_path %>